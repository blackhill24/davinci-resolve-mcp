---
description: Orchestrate (ingest → deliver) in the DaVinci Resolve MCP — a task spans multiple domains (edit AND grade AND deliver) and needs to survive a context reset across sessions — a full ingest-to-delivery post job
alwaysApply: false
---

<!-- GENERATED by scripts/agent-rules/generate.mjs — edit that, not this file. -->

Resolve Orchestrate (ingest → deliver) work. Apply when: a task spans multiple domains (edit AND grade AND deliver) and needs to survive a context reset across sessions — a full ingest-to-delivery post job.

Two servers — compute offline, apply live:
- Live (running Resolve, `davinci-resolve`): orchestrate (start_job, job_status, run_stage, approve_gate, check_resume, rollback_stage, finish_job), delegating to media_pool/media_analysis/auto_edit/timeline/timeline_item_color/render/timeline_markers
- Offline (.drp/.drt/.drx + DB, no Resolve, `davinci-resolve-advanced`): none yet — offline compute-then-apply is a deferred follow-on epic
- Key rule: The sole reason this is a tool and not host prose: surviving context death mid-job. Fingerprint-bound gates — a drifted approval auto-voids. G1 on a talking-head edit ADOPTS auto_edit.approve_cut verbatim; G2 always needs a real look at a rendered frame, never blind. A failed reversible stage never auto-rolls-back — rollback_stage is explicit.

Depth: docs/kernels/orchestration-kernel.md. On-demand in any MCP client: the `/orchestrate_workflow` slash prompt. Claude Code skill: `resolve-orchestrate`. Never modify/transcode/derive source media (AGENTS.md).
